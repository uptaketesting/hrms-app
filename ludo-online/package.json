{
  "name": "ludo-online",
  "version": "1.0.0",
  "description": "Real-time multiplayer Ludo game with server-authoritative architecture",
  "private": true,
  "scripts": {
    "install:all": "npm install && cd client && npm install && cd ../server && npm install",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && nodemon --watch src --watch config --watch ../shared src/server.js",
    "dev:client": "cd server && nodemon --watch ../client src/server.js",
    "start": "cd server && node src/server.js",
    "lint": "eslint client/src server/src shared",
    "test": "jest --coverage",
    "test:watch": "jest --watch"
  },
  "keywords": [
    "ludo",
    "multiplayer",
    "websocket",
    "game"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^8.56.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
